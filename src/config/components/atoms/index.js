/*
 * Barrel export for Eon SFC atoms.
 *
 * Only atoms with no equivalent in the legacy SPA live here.
 * Reusable styling otherwise comes from /config/styles/components.css
 * via the .eon-* utility classes.
 */

export { default as Chip } from '/config/components/atoms/Chip.vue'
