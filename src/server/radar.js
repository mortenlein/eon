import send from 'koa-send'
import { builtinRootDirectory } from './helpers/paths.js'

export const registerRadarRoutes = (router) => {
	// Radar routing moved to central fallback middleware in index.js

}
